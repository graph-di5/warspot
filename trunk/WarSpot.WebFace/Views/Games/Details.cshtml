@using WarSpot.WebFace.Security
@model WarSpot.WebFace.Models.GameModel

@{
}

<!--
	<h3>@Html.DisplayForModel(Model)</h3>
	-->
<div class="editor-label"><b>@Html.LabelFor(m => m.Id)</b></div>
<div class="editor-field">@Html.DisplayFor(m => m.Id)</div>
<div class="editor-label"><b>@Html.LabelFor(m => m.Replay)</b></div>
<div class="editor-field">@Html.DisplayFor(m => m.Replay)</div>
<p>@Html.ActionLink("Скачать", "Download", new {id = Model.Id})</p>
@Html.ActionLink("Проиграть", "Play", new { id = Model.Id })
<!--
	 
  -->