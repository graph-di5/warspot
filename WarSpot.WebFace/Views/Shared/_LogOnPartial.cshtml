@if(Request.IsAuthenticated) {
    <text>Привет, <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Выход", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Вход", "LogOn", "Account") | @Html.ActionLink("Регистрация", "Register", "Account")  ]
}
